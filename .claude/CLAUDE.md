# Instructions

1. To use python, always use uv virtual environment and the related commands (e.g., uv pip install, uv run python, etc.)
2. Whenever writing python codes, use `ruff` linter to ensure style consistency.
